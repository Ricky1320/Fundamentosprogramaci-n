#include <iostream>
using namespace std;

int main()
{
    int n, dig, suma = 0;
    cout << "ingrese el numero";
    cin >> n;
    while(n>0) {
        dig = n % 10;
        n = n / 10;
        suma = suma + dig;
    }
    cout << "la suma total es" << suma;
    return 0;
}
